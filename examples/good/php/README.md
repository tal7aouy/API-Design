# Good PHP API Example

This folder shows a lightweight, Laravel-inspired structure without the full framework:

- `Domain/` holds services, repositories, and DTOs
- `Http/Controllers` maps HTTP to service calls
- Clear separation of concerns and a consistent error contract

The `examples/good/php/bootstrap.php` file demonstrates a minimal router and request dispatch.
